overwrite: true
schema: "graphql/schema.graphql"
# documents: "pages/**/*.graphql"
generates:
  codegen/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
