overwrite: true
schema: "graphql/schema.graphql"
documents: "**/*.tsx"
generates:
  codegen/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      scalars:
        UUID: string
        BigInt: number
        Date: Date